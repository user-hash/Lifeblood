# Tool Invariants

Per-tool semantic contracts: dead-code reachability, invariant
introspection, authority report, forwarder classification, execute
robustness, Unity reachability, find-implementations.

## Dead-Code Analysis (v0.6.3, major fix v0.6.4)

- **INV-DEADCODE-001. `lifeblood_dead_code` walks the graph for symbols with zero incoming non-Contains edges. Every response carries `status` + `warning` fields.** The analyzer checks `graph.GetIncomingEdgeIndexes(sym.Id)` and also checks outgoing `Implements` edges as proof of liveness (a method implementing an interface is reachable through the interface). Self-analysis tail after the post-v0.6.4 extractor pass: ~7 findings (from 150 pre-v0.6.4), all in the known-structural-limitation set below.

  **Closed false-positive classes** (v0.6.4 + post-v0.6.4 extractor pass). Each maps to one or more extractor changes in `RoslynEdgeExtractor`:
  - **Interface dispatch.** `ExtractInheritanceEdges` emits method-level `Implements` edges via `FindImplementationForInterfaceMember` + `AllInterfaces`. Dead-code analyzer checks outgoing `Implements` as proof of liveness.
  - **Member access granularity.** `ExtractMemberAccessEdge` + `ExtractReferenceEdge` emit both type-level and symbol-level `References` edges for properties/fields/method-groups via `EmitSymbolLevelEdge` shared helper. Covers bare field identifiers and method-group references (`new Lazy<>(Load)`, `event += Handler`).
  - **Null-conditional property access.** `MemberBindingExpressionSyntax` handler emits type-level + symbol-level edges for `obj?.Property`.
  - **Lambda / local-function context.** `FindContainingMethodOrLocal` skips lambda and local-function syntax nodes. Calls inside `.Select(x => Foo(x))` attribute to the enclosing named method.
  - **Implicit global usings.** `ModuleInfo.ImplicitUsings` discovered from csproj; `ModuleCompilationBuilder.CreateCompilation` injects a synthetic global-using tree when enabled. Closes the 42% `GetSymbolInfo` null-resolution class (without this, `List<>`, `Dictionary<>` etc. emit CS0246 on every module). Follows INV-COMPFACT pattern.
  - **Constructor `Calls` edge.** `ExtractConstructorCallEdge` emits BOTH a type-level `References` edge (module-coupling signal) AND a method-level `Calls` edge to the `.ctor`. `find_references` on any constructor returns its construction sites.
  - **Field-initializer containing method.** `FindContainingMethodOrLocal` resolves a reference inside `static T _x = Bar()` / `T _x = Bar()` to the type's synthesized `.cctor` / first `.ctor` via `StaticConstructors` / `InstanceConstructors`.
  - **Property accessor context.** `FindContainingMethodOrLocal` returns the accessor `IMethodSymbol`; `GetMethodId` maps `AssociatedSymbol` to the property/event id so the emitted edge source matches the extracted graph node. Covers bodied accessors, expression-bodied properties, and indexer expression bodies.

  **Remaining known false-positive classes (structural, not fixable by static analysis):**
  - **Runtime entry points.** `Program.Main` and `Program` types in composition roots. Self-analysis: 6 findings.
  - **Unity reflection-based dispatch.** Methods called by the Unity engine at runtime via `[RuntimeInitializeOnLoadMethod]`, lifecycle messages (`OnAudioFilterRead`, `OnApplicationFocus`), and `SendMessage`-dispatched handlers. Roslyn cannot see these call sites. Full audit on a real 75-module Unity workspace: 96% true-positive rate (25/26 verified).

  Pinned by `Handle_DeadCode_Response_IncludesExperimentalWarning` plus 26 extractor/analyzer tests in `RoslynExtractorTests` covering each closed FP class.

- **INV-DEADCODE-TRIAGE-002. Dead-code findings carry `sameClassConsumerCount`; `DeadCodeOptions.IncludeSameClassOnlyConsumers` opt-in surfaces symbols whose only consumers share the candidate's containing type.** Additive to INV-DEADCODE-TRIAGE-001. Default mode keeps the strict liveness filter — any symbol with an incoming non-Contains edge is excluded — so the count is `0` for every classic finding. When `IncludeSameClassOnlyConsumers=true`, the analyzer relaxes the filter to also surface symbols whose every incoming non-Contains reference originates from a member with the same `ParentId` (same containing type); the finding then reports the count plus a `Reason` of `"<scope><kind> with only same-class consumers (<N>)"`. Cross-class consumers (even one) suppress the relaxed surfacing — the symbol is genuinely consumed outside its declaring class and not a class-internal cleanup candidate. `IsLiveByOutgoingImplements` is split out of the legacy `HasIncomingReference` helper so the relaxation cannot accidentally surface interface implementers. Use case: class-level cleanup triage. A private field, property, or method whose only consumers are sibling members of the same class is a valid candidate for inlining or removal during a focused class-by-class pass, even though it is semantically alive. `SameClassConsumerCount` is always populated on the wire so consumers can sort/filter, even when the relaxation is off (returns `0` in classic mode). Pinned by `AnalysisToolsTests.DeadCode_DefaultMode_PrivateFieldWithSameClassConsumer_IsNotFlagged` (default filtering preserved), `DeadCode_RelaxedMode_PrivateFieldWithSameClassConsumer_IsFlaggedWithCount`, `DeadCode_RelaxedMode_PrivateFieldWithCrossClassConsumer_IsNotFlagged`, and `DeadCode_TrulyUnusedPrivateField_ReportsZeroSameClassConsumers`. LB-TRACK-20260515-015 / F2 atom of the 2026-05-19 plan.

- **INV-DEADCODE-TRIAGE-001. Every dead-code finding carries triage fields: `directDependants`, `bucket`, `declarationOnly`.** First-response triage shape for a tool whose raw findings list overwhelms agents on real workspaces. `directDependants` is the count of incoming non-Contains edges (always `0` for classic findings the analyzer emits, but on the wire as forward-compatible signal for future relaxed-criteria modes — reachable-only-via-Implements, reachable-only-via-Override — where the "barely reachable" class would surface with a non-zero count). `bucket` is the segment-aware path classification (`Production` / `Test` / `Editor` / `Generated`) mirroring the `blast_radius groupBy=bucket` taxonomy so a caller can join the two tool surfaces; classification is performed on whole path segments (not substrings) and follows precedence Generated → Test → Editor → Production (most-authoritative signal wins). Test beats Editor by design: a fixture under `Tests/Editor/Foo.cs` is a test fixture (defined by its `Tests` root plus filename convention), not an Editor utility — the `Editor/` subfolder there is just NUnit PlayMode assembly placement. Generated wins over everything because build artifacts (`obj/`, `bin/`) and codegen (`*.Generated.*`, `/generated/`) are never a refactor target regardless of any other signal. `declarationOnly` is true iff the underlying `Symbol.IsAbstract` is set — abstract methods, abstract types, interface members; deleting one is a public-contract change for every implementor and must not be treated as a routine cleanup. The response additionally carries a `bucketBreakdown` count map alongside the existing `kindBreakdown` so a caller can fold the giant Editor/Generated tail without re-walking `findings[]`. Computed once per finding at `LifebloodDeadCodeAnalyzer.FindDeadCode` time. Pinned by `DeadCodeTriageFieldsTests` (bucket-classification theory + DeclarationOnly + DirectDependants invariant). Closes LB-TRACK-004.

## Invariant Introspection (v0.6.3, tree-walker added LB-FR-023)

- **INV-INVARIANT-001. Architectural invariants live across well-known repo conventions; the tool aggregates them at runtime.** `lifeblood_invariant_check` discovers source files dynamically via `IFileSystem` — `<root>/CLAUDE.md`, `<root>/AGENTS.md`, and any `<root>/docs/invariants/**.md` tree — parses each with `Internal.ClaudeMdInvariantParser`, caches per-file in `InvariantParseCache<T>` (timestamp-based invalidation), and aggregates results across all sources. Five authoring shapes recognised: A (`- **INV-X-N**: body`), B (`- **INV-X-N. Title.** Body`), C (`**INV-X-N: Title.** body` — bare bold paragraph, no bullet), D (`- **INV-X-N** (vX.Y.Z): body` — parenthesized version tag), E (`- **INV-X-N:** body` — colon inside the bold). Three modes via `IInvariantProvider`: `id` lookup (full body), `audit` (total + per-category breakdown + duplicate ids + parse warnings + every contributing source path in `SourcePaths[]`), `list` (id+title index, no bodies). Parser is a pure function; provider is a thin orchestrator over discovery + cache + parser — adding a new invariant source layout (YAML companion file, GitHub issue tracker, external governance DB) ships as a sibling provider reusing the cache. Duplicate ids declared across files are detected and surfaced. Pinned by `ClaudeMdInvariantParserTests`, `InvariantProviderAndHandlerTests`, `LifebloodClaudeMdSelfTests`, `InvariantParseCacheTests`. Closes LB-BUG-017 / LB-BUG-018 / LB-FR-023.

## Port Health (post-v0.7.7 F3a + F3b)

- **INV-PORT-HEALTH-ANALYZER-SEAM-001. `lifeblood_port_health` runs behind the `IPortHealthAnalyzer` Application port; the algorithm body does NOT live in `ToolHandler`.** Pre-F3a, the ~75-line algorithm sat inline in `ToolHandler.HandlePortHealth` (lines 812-886) — the only Lifeblood tool whose body bypassed the standard Application-port indirection that the other 26 read-side tools already followed. The implementation is now `LifebloodPortHealthAnalyzer` in `Lifeblood.Connectors.Mcp`, behind the `IPortHealthAnalyzer` port in `Lifeblood.Application.Ports.Right`. `ToolHandler` injects the port (default constructor falls back to `new LifebloodPortHealthAnalyzer()` to match the prior `IAuthorityReporter` pattern) and the tool handler shrinks to argument parsing + result shaping. Behavior is byte-equal at the seam atom: same direct-Contains member walk, same liveness rule (incoming non-Contains edge OR outgoing `Implements`), same verdict bands (`empty` / `healthy` ≥0.75 / `mixed` ≥0.25 / `vestigial`). Composite / inherited-interface traversal lands as `INV-PORT-HEALTH-COMPOSITE-001` (F3b). Pinned by `PortHealthAnalyzerTests` (9 fixtures including `Seam_ToolHandler_DoesNotEmbedAnalyzerBody` which scans `ToolHandler.cs` for algorithm-specific tokens — `"vestigial"` string literal, `liveCount++`, `var memberIds = new List<string>` — and refuses to find any). LB-TRACK-20260519-022 / F3a atom of the 2026-05-19 plan.

- **INV-PORT-HEALTH-COMPOSITE-001. `port_health` reports direct + inherited interface surface; liveness verdict is computed across the aggregate.** Pre-F3b, the analyzer counted only directly-declared members reached by outgoing `Contains` edges. Composite interfaces — types that inherit from one or more sub-interfaces and declare little or no surface themselves — appeared as `empty` even when their inherited contracts carried real load-bearing surface. F3b walks outgoing `EdgeKind.Inherits` edges transitively from the target type (the shared `Lifeblood.Domain.Graph.InterfaceInheritanceWalker`, also used by `authority_report` post-F3e), collecting every reachable interface's direct members (distinct by canonical id), and computes verdict + `liveMembers` + `live[]` / `dead[]` across the union of (direct ∪ inherited). Real-graph wire shape requires INV-EXTRACT-IFACE-INHERIT-001 — F3b's synthetic-graph fixtures pre-dated that fix and F3d closes the blind spot. Five new wire fields on `PortHealthReport`: `directMemberCount` (pre-F3b count, what this type contributes itself), `inheritedMemberCount` (distinct ids reachable through the inheritance closure), `aggregateMemberCount` (sum), `inheritedInterfaces[]` (canonical ids, sorted ordinal so the wire shape is deterministic), `isCompositeInterface` (true iff `inheritedMemberCount > 0`). `memberCount` retains its pre-F3b name as a backwards-compatible alias for `aggregateMemberCount` — older callers still get a verdict-relevant total without code change. Pinned by `PortHealthAnalyzerTests` (6 F3b fixtures: non-composite zero-inherited, composite with all-live inherited, composite mixed-live inherited, transitive grandparent walk distinct across closure, empty composite reports `empty`, composite with own direct members aggregates both surfaces). LB-TRACK-20260519-022 / F3b atom of the 2026-05-19 plan.

## Authority Report + Forwarder Classifier (v0.6.7 P5, post-v0.7.7 F3e)

- **INV-AUTHORITY-001. Authority report quantifies type-level architectural responsibility.** `IAuthorityReporter.Analyze(graph, typeId)` returns implementedInterfaceCount (distinct interfaces directly satisfied — outgoing `Implements` edges for class/struct sources, outgoing `Inherits` edges for interface sources per INV-EXTRACT-IFACE-INHERIT-001), ownedPublicSurface (public method/property/field count from `Contains` edges, nested types excluded), per-implemented-interface usage (direct + inherited member counts + distinct consumers reaching the interface or any of its members via `Calls`/`References`, see INV-AUTHORITY-COMPOSITE-001), and a forwarderRatio (`PureForwarderCount / TotalMethodCount` in [0.0, 1.0]; sentinel `-1.0` when no method has the extractor-recorded `classification` property). Single graph walk produces every field. The reporter is stateless (INV-ANALYSIS-001), reads only the graph, and lives in `Connectors.Mcp` because `Lifeblood.Analysis` cannot reference Application ports (INV-CONN-001). Surfaced as the `lifeblood_authority_report` MCP tool. **Use cases are general — not extraction-specific.** Any type that aggregates surface across multiple subordinates is in scope: partial-class hosts, dispatchers that route across many row paths (filter family selectors, kernel routers, capability dispatchers), facade types fronting an internal subsystem, ports with many implementations. The original dogfood case was a partial-class wave on a real Unity workspace; the same metrics drive every other host-with-many-subordinates triage. Pinned by `AuthorityReporterTests.AuthorityReport_*`.

- **INV-AUTHORITY-PLANNING-COMPOSITION-001. Planning verdicts (EvictableDebt / BoundaryContract / SceneDiscoveryContract / CompositeFacade / AdapterShimOnly / NeedsAudit) compose CLIENT-SIDE from the evidence fields `authority_report` returns; Lifeblood ships measurements, not verdicts.** Pre-S7 a caller needed three round trips (authority_report + dependants + find_implementations) to triage whether an interface was a real boundary contract or an evictable adapter shim. S7 adds three evidence fields to the top-level `AuthorityReport`: `crossAssemblyConsumerCount` (distinct other modules with any incoming Calls/References edges into the type or its direct members), `sameAssemblyConsumerCount` (distinct same-module consumer symbols), `hasSingleImplementer` (true/false for interface targets with exactly one source-defined Implements edge; null for non-interface targets). Combined with the F3e per-interface `aggregateMemberCount` + the existing `forwarderRatio` + `ownedPublicSurface`, callers derive verdicts: **BoundaryContract** = high `crossAssemblyConsumerCount` + low `forwarderRatio` + multiple implementers (or none-yet-by-design); **AdapterShimOnly** = zero `crossAssemblyConsumerCount` + high `sameAssemblyConsumerCount` + `hasSingleImplementer == true`; **CompositeFacade** = `PerInterface[*].isCompositeInterface == true` carrying the load-bearing surface; **EvictableDebt** = `forwarderRatio > 0.6` + low `ownedPublicSurface` + concentrated same-assembly use; **SceneDiscoveryContract** = Unity workspace + zero cross-assembly use but the type carries Unity attributes (caller checks `Symbol.Properties["attributes"]`); **NeedsAudit** = no obvious shape fits, evidence is mixed. The recipe mirrors INV-FLAG-COVERAGE-COMPOSITION-001's precedent — Lifeblood is an instrument, not an opinion engine; verdict-shaped wire tools are forbidden so the caller's domain knowledge stays load-bearing. Evidence computation lives in `LifebloodAuthorityReporter.ClassifyConsumersByAssembly` + `ComputeHasSingleImplementer` + `FindContainingModule` (walks `Symbol.ParentId` up to a Module ancestor with a 16-hop safety cap). Pinned by 8 S7 fixtures in `AuthorityReporterTests`: boundary-shape (cross > 0, same = 0), adapter-shim-shape (cross = 0, same > 0), no-module-ancestor degrades to (0, 0), hasSingleImplementer true for one-impl interface, false for multi-impl, null for non-interface, null for missing typeKind, real-graph end-to-end through the production extractor. LB-TRACK-20260519-027 / S7 atom of the 2026-05-19 plan.

- **INV-AUTHORITY-COMPOSITE-001. `InterfaceUsage` rows on an authority report carry direct + inherited member surface; composite interface facades aggregate inherited contracts.** Pre-F3e, each per-interface row reported only the satisfied interface's directly-declared members (outgoing `Contains` edges from the interface itself). For ABG-style composite facades — an interface that declares little or no surface of its own but inherits 2-N sub-interfaces — this gave `MemberCount: 0` and made the row look architecturally empty even when the inherited contract carried the real load-bearing surface. F3e reuses the shared `InterfaceInheritanceWalker` (also used by `port_health`'s F3b) to walk the satisfied interface's transitive `EdgeKind.Inherits` closure and sum each parent's direct member count. Five new wire fields on `InterfaceUsage`: `directMemberCount` (pre-F3e count, what the satisfied interface contributes itself), `inheritedMemberCount` (distinct member ids reachable through the inheritance closure), `aggregateMemberCount` (sum), `inheritedInterfaces[]` (canonical ids, sorted ordinal so the wire shape is deterministic), `isCompositeInterface` (true iff `inheritedMemberCount > 0`). `memberCount` retains its pre-F3e name as a backwards-compatible alias for `aggregateMemberCount`. Consumer counting also walks the aggregate: a caller of an inherited member counts as a consumer of the composite facade (it reaches the contract). Pinned by `AuthorityReporterTests.AuthorityReport_CompositeInterfaceFacade_AggregatesInheritedSurface`, `AuthorityReport_NonCompositeInterface_ReportsZeroInheritedSurface`, `AuthorityReport_CompositeInterfaceConsumers_CountAcrossAggregate`, `AuthorityReport_InterfaceTypeAsHost_FindsParentInterfacesPostF3c`, and `AuthorityReport_RealGraph_CompositeHost_AggregatesInheritedMembers` (real-graph end-to-end pin via the production extractor pipeline). LB-TRACK-20260519-022 / F3e atom of the 2026-05-19 plan.

- **INV-FORWARDER-001. Method body shape is classified at extraction time, recorded on `Properties["classification"]`.** Three categories: `PureForwarder` (single-statement / expression-bodied method whose body is exactly one invocation expression), `ThinWrapper` (≤ 5 statements containing exactly one invocation overall — null-guard-then-delegate, simple-cast-then-delegate), `RealLogic` (anything else). Abstract / partial / extern methods are not classified (no entry written) — there's no body shape to categorize. Heuristic, not semantic — recorded on the type's own member, not the call target — but stable enough that an authority report can compute a coarse forwarder ratio without a second walk. Detected by `RoslynSymbolExtractor.AttachMethodClassification` (Phase P5 / LB-FR-020). Pinned by `AuthorityReporterTests.Extractor_*`.

## Execute Robustness (v0.6.7 P4)

- **INV-EXECUTE-001. `lifeblood_execute` has one reference-admission seam: `Internal.ScriptReferenceSetBuilder`.** Every execute call uses the host scripting BCL, retained workspace `CSharpCompilation` metadata references, admitted runtime probe DLLs, and the assemblies that define `RoslynSemanticView` globals. `host` is the only execution profile because scripts run in-process and share retained Roslyn workspace state; non-host `targetProfile` values are accepted for compatibility, still run against the host scripting BCL, and surface a `targetRuntimeWarnings` limitation instead of pretending to validate against a ref-pack. When the workspace is Unity-shaped (`Library/` exists at the project root), the `IRuntimeAssemblyResolver` adapter (`Lifeblood.Adapters.CSharp.UnityAssemblyResolver`) probes `Library/ScriptAssemblies/`, `Library/Bee/artifacts/`, and `Library/PackageCache/` so scripts can touch `UnityEngine` types; an empty `Library/` surfaces a `runtimeAssemblyWarnings` entry telling the caller to run a Unity build first. Runtime probe candidates pass through `System.Reflection.AssemblyName.GetAssemblyName(path)` before becoming `MetadataReference`s. Native PEs (Unity IL2CPP `GameAssembly.dll`, C++/CLI without managed surface, etc.) throw `BadImageFormatException` and are filtered at the reference-graph boundary; runtime BCL/contract assemblies (`mscorlib`, `netstandard`, `System.*`, etc.) are filtered so Unity player/stripped framework artifacts cannot shadow the host script core library; duplicate assembly identities collapse to one runtime reference; assemblies already represented by retained workspace compilations are skipped. The script-globals object (`RoslynSemanticView`) ships `Help`, `SymbolsOfKind(string)`, and `EdgesOfKind(string)` so scripts can introspect the sandbox without importing the `SymbolKind` / `EdgeKind` enums. Pinned by `ExecuteRobustnessTests` — resolver no-Library / library-without-artifacts / DLL discovery; host and non-host target-profile warning behavior; warnings propagation through success and blocked-pattern paths; native-PE and runtime-BCL filtering; runtime identity deduplication; legacy-overload delegation; `Help` content shape; string-named `SymbolsOfKind` / `EdgesOfKind` filtering. Closes LB-BUG-014, LB-BUG-007, LB-FR-012, LB-FR-013, LB-BUG-008, LB-LIM-004(a/b).

## Unity Reachability (v0.6.7 P3)

- **INV-UNITY-001. Runtime-dispatch reachability is a typed port. `IDeadCodeAnalyzer` consults `IUnityReachabilityProvider` (when injected) before flagging a symbol as dead.** The reference adapter (`Lifeblood.Adapters.CSharp.UnityReachabilityAdapter`) recognizes Unity entrypoint attributes (`RuntimeInitializeOnLoadMethod`, `InitializeOnLoadMethod`, `MenuItem`, `ContextMenu`, `PostProcessBuild`, `PostProcessScene`, `CustomEditor`, `CustomPropertyDrawer`, `Test`, `UnityTest`, …) and MonoBehaviour magic methods on types whose inheritance chain reaches a Unity message-receiver base (`UnityEngine.MonoBehaviour`, `UnityEngine.ScriptableObject`, `UnityEditor.Editor`, `UnityEditor.EditorWindow`, `UnityEngine.StateMachineBehaviour`). The adapter walks the chain via the extractor-recorded `Symbol.Properties["baseType"]` (FQN) so chains terminating in external types not loaded into the graph (UnityEngine.dll lives outside the workspace) still resolve. Cycle-safe with a 32-hop cap. Composition root injects the adapter by default; non-Unity workspaces see zero behavior change because nothing matches. Dogfooded on a real 87-module Unity workspace: dead_code findings 1095 → 729 (-33%), MonoBehaviour-magic FPs 378 → 13 (-97%). Pinned by `UnityReachabilityTests` (15 tests) covering attribute matching, magic-method detection on direct + transitive subclasses (in-graph and via external base FQN), negative cases, cycle-safety, extractor `Properties["baseType"]` + `Properties["attributes"]` recording, and end-to-end through `LifebloodDeadCodeAnalyzer`. Closes LB-FP-001, LB-FP-002, LB-BUG-009.

- **INV-UNITY-002. Asmdef edits force a full incremental re-analyze.** `AnalysisSnapshot.AsmdefTimestamps` records `*.asmdef` mtime at full-analyze time. `RoslynWorkspaceAnalyzer.IncrementalAnalyze.HasAsmdefDrift` re-scans the project root on every incremental call; any added, edited, or removed `.asmdef` triggers a full re-analyze that round. Necessary because Unity csprojs are generated from asmdefs — editing an asmdef without forcing Unity to regenerate the csproj leaves the csproj-timestamp tracker (INV-BCL-005) blind to the change, which silently runs analysis against stale module facts. Pinned by `UnityReachabilityTests` end-to-end coverage; closes the silent-stale-module class promoted from LB-NICE-003.

## Semantic Search Match Source

- **INV-SEARCH-MATCHKIND-001. `SearchResult.MatchKind` structurally reports which scoring bucket(s) drove the rank.** The reference adapter (`Lifeblood.Connectors.Mcp.LifebloodSemanticSearchProvider`) ranks symbols against three buckets — bare `Name`, `QualifiedName` (exclusive of Name per the bare-name guard), and persisted `xmlDocSummary`. Pre-fix the bucket signal lived only in the human-rendered `MatchSnippets` strings (`"name: …"` / `"qualifiedName: …"` / `"xmlDoc: …"`); callers who wanted to filter by source had to parse those strings. The first-hit-token trackers inside the adapter already knew which buckets fired — INV-SEARCH-MATCHKIND-001 lifts that to a typed `MatchKind` enum on `SearchResult`. Values: `Name`, `QualifiedName`, `XmlDoc` for single-bucket hits; `Multiple` when more than one bucket fired (e.g. token in both bare name and xmldoc summary, or qualified name and xmldoc summary). The enum is adapter-agnostic — future TypeScript / Python search adapters reuse the same taxonomy (their corpora carry the same conceptual buckets). Snippet strings are unchanged so existing renderers keep working; the structured field is purely additive. Pinned by `SemanticSearchTests.Search_*MatchKind*` (5 cases: name-only, qualifiedName-only-with-bare-name-guard, xmldoc-only, name+xmldoc Multiple, qualifiedName+xmldoc Multiple).

## Find-Implementations Comparison (v0.6.1)

- **INV-FINDIMPL-001. `FindImplementations` compares via canonical Lifeblood symbol IDs, never display strings or `SymbolEqualityComparer`.** The canonical ID from `RoslynCompilationHost.BuildSymbolId` (routing through `CanonicalSymbolFormat`) is the only cross-assembly-safe comparison for C# symbols. `ToDisplayString()` is subject to the v0.6.0 nullability/reduced-name/attribute drift class. `SymbolEqualityComparer.Default` is stricter than needed — treats source and PE-downgraded metadata copies as unequal because they live in different assemblies, breaking the cross-module matching `FindImplementations` needs. This invariant generalizes to ALL write-side matching across the source/metadata boundary: always route through `BuildSymbolId`. Pinned by `WriteSideIntegrationTests.FindImplementations_IGreeter_FindsGreeterAndFormalGreeter`.


## Member Resolution (post-v0.7.2)

- **INV-RESOLVE-MEMBER-001. `lifeblood_resolve_member` is a type-scoped counterpart to `lifeblood_resolve_short_name`.** Where short-name resolution flattens every member matching a bare name across the entire graph (often 16+ hits on common names like `Dispose` / `Update`), `ResolveMember(graph, typeName, memberName, paramTypes?)` scopes to ONE specific containing type and returns the member surface on THAT type only. The `typeName` argument accepts canonical (`type:NS.T`), fully-qualified (`NS.T`), or bare short name (`T`); bare names dispatch through the short-name index. When the bare short name resolves to multiple types, `outcome = AmbiguousContainingType` with every candidate id returned for caller disambiguation — the resolver NEVER silently picks. The `paramTypes[]` argument filters method overloads by parameter signature (each entry is one fully-qualified parameter type); ignored for non-method members. Typed outcome enum: `Unique` / `MultipleMatches` / `NotFound` / `TypeNotFound` / `AmbiguousContainingType`. Port: `ISymbolResolver.ResolveMember` + `MemberResolutionResult`. Adapter: `LifebloodSymbolResolver.ResolveMember`. Pinned by `ResolveMemberTests` (12 cases covering every `ResolveMemberOutcome`). Closes the field-report 2026-05-11 P1 type-scoped-member ask.

## Blast-Radius Grouping (post-v0.7.2)

- **INV-BLAST-RADIUS-GROUP-001. `lifeblood_blast_radius` supports caller-selected response shapes for large transitive sets.** Default `groupBy = "none"` preserves the legacy flat shape (`affected[]` array + `summarize` / `maxResults` truncation). Caller passes `groupBy = "bucket" | "module" | "both"` to switch to grouped buckets (`byBucket: { Production, Test, Editor, Generated }`) and / or per-module counts (`byModule: { <moduleName>: { count, preview[] } }`) with optional per-group preview entries capped by `previewPerGroup` (default 5; `0` = counts only). Mutually exclusive with `summarize: true`. Bucketing is derived from file-path prefix (`Editor/`, `Tests/`, `.Generated.`) — no separate symbol-side metadata, so the policy generalises across consumer projects with conventional folder layouts. The flat-list shape returned a multi-megabyte response on transitive blasts of popular types; the grouped shape answers "how much production code breaks if I touch X?" in one number. Pinned by `BlastRadiusGroupingTests`. Closes the field-report 2026-05-11 P1 bucketing ask.

## Enum Coverage (post-v0.7.3, post-v0.7.7 S4)

- **INV-ENUM-COVERAGE-001. `lifeblood_enum_coverage` returns per-member reference coverage for an enum type — `produced` / `consumedComparison` / `consumedSwitch` / `other` counts classified by parent syntax, plus convenience flags `isUnproduced` (declared + referenced as a consumer but never assigned) and `isUnreferenced` (zero references of any kind).** Pre-fix a caller had to pair `find_references` with manual syntax inspection per hit to answer "is this state-machine value ever assigned, or only checked for?" — the dogfood case is a telemetry / reject-reason enum that drifts when a value is checked-for but no producer ever surfaces. The classifier walks the parent chain from each enum-member reference site: a parent of `AssignmentExpressionSyntax.Right`, `EqualsValueClauseSyntax`, `ReturnStatementSyntax`, `YieldStatementSyntax`, `ArrowExpressionClauseSyntax`, or `ArgumentSyntax` counts as Produced; `BinaryExpressionSyntax` with `==`/`!=`/`<`/`<=`/`>`/`>=` counts as ConsumedComparison; `IsPatternExpressionSyntax`, `ConstantPatternSyntax`, `CaseSwitchLabelSyntax`, `CasePatternSwitchLabelSyntax`, `SwitchExpressionArmSyntax` count as ConsumedSwitch. The walk surfaces the enclosing `MemberAccessExpressionSyntax` first (so `FieldMask.ShimmerPhase` classifies the whole expression, not just the bare identifier token), then climbs until it hits a classifying node or leaves expression context (statement / member declaration ancestor → Other bucket). Single O(total_nodes) pass per compilation — cheaper than calling `FindReferences` per-member on big enums (N × full-tree walk each). Top-level `unproducedCount` + `unreferencedCount` summarize the response so the dogfood case reads off the top of the wire. `enumTypeId` accepts canonical, fully-qualified, or bare short name — routed through `ISymbolResolver` the same way every other type-id-taking tool resolves. Domain: `EnumCoverageReport { EnumTypeId, EnumTypeName, Members[], UnproducedCount, UnreferencedCount }` + `EnumMemberCoverage { MemberId, Name, TotalReferences, ProducedCount, ConsumedComparisonCount, ConsumedSwitchCount, IsUnproduced, IsUnreferenced }`. Port: `ICompilationHost.GetEnumCoverage(string enumTypeId) → EnumCoverageReport?` (null when the id does not resolve to an enum). Adapter: `RoslynCompilationHost.GetEnumCoverage` plus private `EnumRefClass` taxonomy + `ClassifyEnumReferenceSite`. Handler: `WriteToolHandler.HandleEnumCoverage`. Pinned by `EnumCoverageTests` (8 facts: non-enum returns null, unknown returns null, empty-enum empty-members, production-sites counted, comparison counted, switch+pattern counted, unproduced-flagged-and-counted dogfood scenario, members-in-declaration-order wire stability). Closes LB-TRACK-20260514-003.

- **INV-ENUM-COVERAGE-DISPATCH-TABLE-001. `enum_coverage` reports an additive `dispatchTableReferenceCount` for references that live inside a static-table-shaped initializer; recognition shares the `static_tables` classifier so there's one source of truth.** Pre-S4 the analyzer was purely syntactic — it classified an enum-member reference into Produced / Comparison / SwitchPattern / Other by parent syntax, with no awareness that some `Produced` sites are routing-table cells (`new Capability(Mode.A, HandleA)` in `static readonly Capability[] All = { ... }`) rather than genuine assignments. The blind spot prevented triaging "values that exist only as dispatch keys" from "values genuinely produced in app code". S4 adds `DispatchTableReferenceCount` to `EnumMemberCoverage` — ADDITIVE: every dispatch-table cell still increments its syntactic bucket (typically `ProducedCount`, since argument position is value-producing) AND increments this new counter. A row where `DispatchTableReferenceCount == TotalReferences` is a pure routing key; a row where `DispatchTableReferenceCount == 0 && ProducedCount > 0` is a normal assignment site. Recognition lives in `RoslynStaticTableExtractor.IsInsideStaticTableInitializer(model, node)` — walks syntax ancestors up to either an `EqualsValueClauseSyntax` (`= expr` form, fields + property setters) OR an `ArrowExpressionClauseSyntax` (`=> expr` form, expression-bodied static properties), verifies the enclosing declaration is a `static` field or property, then calls the same private `ClassifyContainer` the `static_tables` tool uses to decide if the initializer shape is `Array` / `CollectionExpression` / `ObjectCreation`. Both initializer shapes are recognised in parity with the static-table extractor's `TryGetInitializerExpression` (which also accepts both — a parity gap caught during S4 self-review). Single SSoT: any future container-shape extension on `static_tables` automatically widens what counts as a dispatch table here. Stops at any enclosing `StatementSyntax` / `AnonymousFunctionExpressionSyntax` so a reference inside a method body or lambda does not falsely match an enclosing field initializer (a lambda body inside `static readonly Func<Mode> F = () => Mode.A;` is its own scope — the lambda IS the dispatch-table cell, not its body). INSTANCE field initializers are excluded (the recognition requires the `StaticKeyword` modifier) so the predicate stays honest about what is and is not a static dispatch table. Pinned by `EnumCoverageTests` (8 S4 facts: object-creation rows count, regular production sites don't count, literal-array initializers count, switch arms don't count, expression-bodied static-property arrow initializers count, lambda bodies inside static initializers don't count, instance-field initializers don't count, dispatch-table-only-member triage scenario). LB-TRACK-20260515-014 / S4 atom of the 2026-05-19 plan.

## Test Impact (post-v0.7.3)

- **INV-TEST-IMPACT-001. `lifeblood_test_impact` reports which test classes transitively depend on a target symbol or file, classified by hop distance and grouped by containing type.** Pre-fix a caller asking "which tests prove that my change to X is safe?" had to pair `lifeblood_blast_radius` with manual test-method discovery (filter the affected list to test-bucket paths, walk back to containing types, compose `dotnet test --filter` strings by hand). The new tool does it in one call. Algorithm: BFS over INCOMING non-Contains edges with per-symbol minimum-distance tracking; first-seen wins because BFS explores in increasing-distance order. Each visited Method symbol is checked against the extractor-recorded `Properties["attributes"]` string — the test-case attribute set is `Test`, `TestCase`, `TestCaseSource`, `Theory`, `UnityTest`, `Fact` (NUnit + Unity Test Framework + xUnit). Lifecycle attributes (`SetUp`, `OneTimeSetUp`, `TearDown`, `OneTimeTearDown`, `UnitySetUp`, `UnityTearDown`) are intentionally excluded — they participate in test execution but are not the assertion-bearing methods a caller wants enumerated. Affected test methods are folded by their containing type (walked via `Symbol.ParentId` chain); per-class `minDistance` is the smallest distance across the class's affected methods, mapped to confidence Direct (1) / OneHop (2) / Transitive (3+). Wire shape: `target`, `targetKind` (Symbol or File), `totalTestMethodCount`, `directTestClassCount`, `affectedTestClassCount`, `affectedTestClasses[]` sorted by ascending distance then by qualified name for byte-stable output, plus `recommendedFilters[]` — pre-composed `FullyQualifiedName~<qualifiedName>` strings the caller pastes into `dotnet test --filter` without composing the syntax themselves. Disambiguation: a `target` value starting with a canonical-id prefix (`type:` / `method:` / `field:` / `property:` / `mod:` / `file:` / `ns:` / `namespace:`) routes through `ISymbolResolver`; otherwise the value is treated as a file path and every symbol declared in that file becomes a multi-source BFS start (single-pass analyzer; distance is the minimum over every file-side source). Lives in `Lifeblood.Analysis/TestImpactAnalyzer.cs` because the walk is pure graph read (INV-ANALYSIS-002) — no Roslyn compilation needed, separate from the write-side semantic tools. Pinned by `TestImpactAnalyzerTests` (9 facts: no-tests empty report, direct-caller Direct confidence, one-hop OneHop confidence, tests grouped by containing type, non-test methods excluded, lifecycle attributes excluded, recognizes UnityTest + xUnit Fact, recommended-filters in distance order, file-mode multi-source BFS). The test-case attribute set is duplicated relative to `UnityReachabilityAdapter`'s entry-point set; that's intentional — UnityReachability uses the broader set for dead-code dispatch-entrypoint detection, this analyzer needs only assertion-bearing methods. Consolidation belongs to a separate atom if the two policies ever diverge. Closes LB-TRACK-20260514-007.

## Cycle Taxonomy (post-v0.7.3)

- **INV-CYCLE-TAXONOMY-001. `lifeblood_cycles` classifies every detected SCC into one of three triage buckets and surfaces both per-cycle `bucket` and aggregate `bucketBreakdown` on the wire.** Pre-fix the tool returned a flat `string[][]` of every cycle Tarjan produced — on a real Unity workspace that ships at 100+ SCCs (DAWG: 123) where the lion's share are not architectural loops at all. The three buckets, ordered by precedence (most authoritative wins): (1) `GeneratedOrStaticAnalysisArtifact` — any participating symbol's file path matches `*.g.cs`, `*.Generated.*`, or a path segment named `obj` / `bin` / `generated`. Build artifacts and source-generator output are never a refactor target, so they short-circuit ahead of every other signal. (2) `PartialClassCluster` — every participating method / property / field walks up the `Contains` reverse-chain to the same enclosing `Type` symbol. Captures intra-type mutual recursion / method-pair cycles (partial classes manifest the same way because Roslyn surfaces them as one type with members spread across files). (3) `LikelyRealLoop` — everything else; the actual cross-type / cross-module architectural-backlog cycles. Implementation: `CircularDependencyDetector.DetectClassified(graph)` reuses the existing Tarjan SCC pass via `Detect(graph)` so cycle membership is byte-identical to the legacy shape — bucket is additive metadata, never a member-set change. The legacy `Detect → string[][]` overload stays for the five existing call-sites (`AnalysisPipeline`, three test fixtures, the context-pack assembler); only `HandleCycles` consumes the classified shape. Wire-shape additive: response now carries `descriptors[] { symbols, bucket }` and `bucketBreakdown { GeneratedOrStaticAnalysisArtifact: N, PartialClassCluster: N, LikelyRealLoop: N }` alongside the legacy `cycles[][]` and (in summarize mode) `previewClassified[]` alongside `preview[]`. Pinned by `CycleTaxonomyTests` (8 facts: cross-type LikelyReal default, obj-segment Generated, `.Generated.` filename Generated, `.g.cs` filename Generated, two-methods-same-type PartialClassCluster, Generated-beats-Partial precedence, membership-matches-legacy-Detect back-compat, empty-graph empty-result). Path-pattern detection lives in the shared `Lifeblood.Domain.PathClassification.PathBucketClassifier` (Domain-layer, zero deps) post-FOLLOWUP-005 — all three former drifted classifiers (`LifebloodDeadCodeAnalyzer.ClassifyBucket`, `LifebloodMcpProvider.ClassifyBucket`, this detector's `IsGeneratedOrStaticAnalysisPath`) now forward to that SSoT. `PathBucket` enum mirrors `DeadCodeBucket` integer values; parity pinned by `PathBucketParityTests`. INV-PATHBUCKET-SHARED-001. Closes LB-TRACK-20260514-008 + LB-FOLLOWUP-20260514-005.

## Path Bucket Classifier SSoT (post-v0.7.3)

- **INV-PATHBUCKET-SHARED-001. Path-prefix classification into `Production` / `Test` / `Editor` / `Generated` lives in one Domain-layer SSoT (`Lifeblood.Domain.PathClassification.PathBucketClassifier`); every consumer forwards through it.** Pre-fix three classifiers had drifted in three places: `LifebloodDeadCodeAnalyzer.ClassifyBucket` (Connectors.Mcp), `LifebloodMcpProvider.ClassifyBucket` (Connectors.Mcp), and `CircularDependencyDetector.IsGeneratedOrStaticAnalysisPath` (Analysis). Each implemented its own segment-walk with subtly different precedence rules (e.g. one walked `Editor/` ahead of `Tests/`, another did the reverse) — exactly the silent-drift class the SSoT pattern exists to prevent. The Domain-layer classifier is a pure function over a file-path string: walks path segments (not substrings, so `Editor` matches `Editor/Foo.cs` but not `MyEditor.cs`), applies precedence `Generated → Test → Editor → Production` (most-authoritative signal wins; Tests-under-Editor reads as Test fixture by design), and returns one of four enum values. Domain layer is the correct home — the rule is data-domain (path strings, not Roslyn semantics or adapter wiring) and has zero outbound deps. The `PathBucket` enum integer values mirror the Connector-layer `DeadCodeBucket` enum so the two stay byte-equal at the wire — pinned by `PathBucketParityTests`. All three former classifiers now forward to `PathBucketClassifier.Classify`; the legacy method bodies stay (one-line `=> PathBucketClassifier.Classify(...)`) so the caller surface is byte-stable but the implementation is single-rooted. Pinned by `PathBucketClassifierTests` (segment-vs-substring, precedence, every bucket case) + `PathBucketParityTests` (enum-value parity with `DeadCodeBucket`). Closes `LB-FOLLOWUP-20260514-005`.

## Static Table Extraction (post-v0.7.4)

- **INV-EXTRACT-STATIC-IMPLICIT-ARRAY-001. Implicit array initializers (`static T[] X = { ... }` with no `new T[]` prefix) classify as `ContainerKind = Array` alongside the explicit `new T[] { ... }` form.** Pre-fix the extractor's `ClassifyContainer` switch only matched `IArrayCreationOperation`; the implicit form surfaces in Roslyn as `IArrayInitializerOperation` whose `Type` is null and whose element values live directly under `.ElementValues`. The empirical class (`LB-INBOX-011` part 1): a 90-module Unity workspace's recipe arrays (`static readonly float[] Weights = { 0.1f, 0.2f }`) were silently skipped because no branch in the dispatch matched that op shape. Closed by an explicit `IArrayInitializerOperation` branch that pulls element values from `op.ElementValues` and resolves the element type from the declaring member's `ITypeSymbol` (threaded into `ClassifyContainer` because the op itself carries no type metadata for the array variant). Container kind stays `Array` because authoring intent is identical between explicit and implicit forms; distinguishing them downstream would create a brittle two-shape contract for one logical construct. Operation-tree only — never regex, never syntax-text. Pinned by `GetStaticTables_StaticImplicitArrayField_DetectedAsArrayContainer` (field form, `float[]` element type round-trips as `type:System.Single`) and `GetStaticTables_StaticImplicitArrayProperty_DetectedAsArrayContainer` (auto-property form, `byte[]` round-trips as `type:System.Byte`).

- **INV-EXTRACT-STATIC-TABLES-001. `lifeblood_static_tables` extracts every `static` field / property initializer on a type whose initializer Roslyn surfaces as a collection-shaped operation (`IArrayCreationOperation` / `IArrayInitializerOperation` / `ICollectionExpressionOperation` / single `IObjectCreationOperation`) and reports row + cell facts sourced from `SemanticModel.GetOperation` only — no syntax-text parsing, no regex, no execution of static constructors.** The dogfood case is a "row-table" (capability table, dispatch table, schema table) where the source-of-truth lives in a static initializer and downstream tools need typed row data to join against contracts. Pre-fix a caller had to walk syntax by hand and pattern-match constructor arguments against parameter names manually — the new tool returns the same shape Roslyn already classified at compile time. **Generic, not consumer-aware:** the extractor MUST NOT reference any consumer-domain vocabulary (project type names, project-specific constants, domain-specific argument names). Pinned by `StaticTableNameLeakageTests` (scans `RoslynStaticTableExtractor.cs` + `StaticTableResults.cs` for a curated forbidden-token list of project-specific identifiers from real-world consumer codebases; any leak fails the build). Cell-kind classification is open for non-breaking extension via `StaticTableValueKind` constants — adding a new kind is non-breaking, renaming or removing one is a contract break. `Computed` is the eternal escape hatch: any operation shape the classifier does not yet match returns `Kind = Computed` with `rawText` carrying the source span as the only provenance. The v1 kind set: `Null` / `Bool` / `String` / `Number` (from `ILiteralOperation.ConstantValue` — numerics narrowed to `double` for wire simplicity, `RawText` preserves integer-precision provenance), `EnumMember` (`IFieldReferenceOperation` against an enum-const field), `EnumFlags` (`IBinaryOperation` with `OperatorKind.Or` recursively flattened into a leaf field-id list in authoring order — both operands must be enum-const refs or the composition falls back to Computed), `MethodGroup` (`IDelegateCreationOperation` over `IMethodReferenceOperation` — caller's method-id only; the extractor does NOT peek inside delegate bodies, that is dataflow and a separate truth tier), `FieldReference` (non-enum static field reference), `Array` (nested `IArrayCreationOperation` / `ICollectionExpressionOperation` recursively classified). `argumentKind` mirrors `IArgumentOperation.ArgumentKind` (`Explicit` / `DefaultValue` / `ParamArray`) so a caller distinguishes author-supplied cells from constructor defaults without round-tripping to source text; newer Roslyn enum cases round-trip via `ToString()` so a version bump cannot crash classification. Row binding: `IObjectCreationOperation.Arguments` already routes named / positional / optional arguments to their `IParameterSymbol` — the cell loop is a direct read of `arg.Parameter` + `arg.ArgumentKind`, no hand-rolled overload resolution. Container resolution finds the owning compilation in the retained `_compilations` dict by metadata-name lookup with a `SymbolEqualityComparer` fallback because `ResolveFromSource` may return a workspace-owned symbol whose `SyntaxTree` references are not reference-equal to retained-compilation trees; member iteration walks the local `INamedTypeSymbol`'s `GetMembers()` so `DeclaringSyntaxReferences` point to trees the compilation actually owns. Truncation envelope: `maxRows` (default 1024) and `maxTables` (default 64) caps fire `RowsTruncated` / `TablesTruncated` flags; zero / negative values clamp to the default so a caller cannot disable extraction by passing a malformed cap. Provenance: every row, cell, and value carries `filePath` + 1-based `line` + 1-based `column`; every value also carries the source-span `rawText` as the eternal Computed-fallback audit trail. Domain: `StaticTableReport { TypeId, Tables[], TablesTruncated }` + `StaticTable { MemberId, MemberName, FilePath, Line, Column, ModuleName, ContainerKind, ElementTypeId?, Rows[], RowsTruncated }` + `StaticTableRow { Ordinal, FilePath, Line, Column, ConstructorId?, Cells[], Value? }` + `StaticTableCell { ParameterName?, Position, ArgumentKind, Value }` + `StaticTableValue { Kind, RawText, FilePath, Line, Column, BoolValue?, StringValue?, NumberValue?, EnumMemberId?, EnumFlagMemberIds[]?, MethodGroupId?, FieldReferenceId?, ArrayElements[]? }` (zero Roslyn dep). Port: `ICompilationHost.GetStaticTables(string typeId, StaticTablesOptions options) → StaticTableReport?` (null when the id does not resolve to a type in source). Adapter: `RoslynCompilationHost.GetStaticTables` routes through `RoslynStaticTableExtractor` (separate internal class so the name-leakage ratchet scopes cleanly). Handler: `WriteToolHandler.HandleStaticTables`. Pinned by `StaticTableExtractorTests` (33 facts: container detection across all three shapes + static property + expression-bodied property, literal cells for every primitive kind, constructor row cells with positional / named / default-arg binding, single-object-creation table, enum-member + composed flags + nested-flag flattening + single-flag-as-member, method-group cells + lambda → Computed, static-field-reference cells, nested-array + nested-collection-expression cells, typeof → Computed, MaxRows + MaxTables truncation flags, empty-initializer zero rows, negative-cap clamps to default) plus `StaticTableNameLeakageTests` (consumer-domain-name ratchet). Closes LB-TRACK-20260514-005.

- **INV-FLAG-COVERAGE-COMPOSITION-001. The "does method M reference every enum-flag in row R's flag-cell?" join composes from `lifeblood_static_tables` (row side) + `lifeblood_dependencies` (method side) with client-side set-relation. Lifeblood MUST NOT ship a one-classification-per-(row,method) wire shape using consumer-interpretation names (`admitted` / `admission` / `inconclusive` / `denied` and equivalents) on this question.** Pre-fix the consumer-side instinct was to ask Lifeblood for a verdict-shaped tool that bakes the consumer's *meaning* of "coverage" into Lifeblood's contract. The instinct is wrong on two counts: (1) the three coverage relations (subset / partial / disjoint) are pure set-relations over two flag-id sets that BOTH existing tools already emit — the row side via `lifeblood_static_tables` (`EnumFlagMemberIds` on `EnumFlags` cells, `MethodReturnFlagIds` on `MethodGroup` cells per `INV-METHOD-FLAG-SUMMARY-001`), the method side via `lifeblood_dependencies` (outbound `References` edges to enum-flag member fields); (2) what "covered" / "partial" / "disjoint" *means* in the caller's domain is a CONSUMER concern, not a Lifeblood concern — committing to a verdict name encodes one consumer's semantics into a tool shipped to many. The eternal recipe: `row.flags := static_tables(typeId, memberName).rows[i].cells["<paramName>"].value.EnumFlagMemberIds ?? value.MethodReturnFlagIds`; `method.flags := dependencies(methodId).Where(d => d.kind == "References" && d.otherEndId.StartsWith($"field:{enumTypeFullyQualifiedName}."))`; the caller computes `row.flags ⊆ method.flags` (full coverage), `row.flags ∩ method.flags == ∅` (disjoint), or otherwise (partial) on their side. `lifeblood_dependencies` strictly supersets `MethodReturnFlagIds` for three cases the v1.1 field nulls: (a) cross-compilation targets — graph edges cross module boundaries while `MethodReturnFlagIds` only walks same-compilation source bodies; (b) flag references in body positions other than `return` — inspection guards, switch arms, ternary conditions; (c) transitive walks through helper-call delegation — the caller follows the `Calls` edge one level deeper if they need closure. Pinned by `INV-METHOD-FLAG-SUMMARY-001` (the row-side emission) plus `EdgeCallSiteTests.Extract_FieldReference_AttachesCallSite` (the method-side `References`-edge emission for field access, which includes enum-member fields). A "make the question one tool call" proposal MUST be rejected unless it proves a Roslyn-grounded surface is needed that neither `static_tables` nor `dependencies` already emits — a re-naming and re-packing of existing facts is not a tool, it is a recipe. Closes LB-TRACK-20260514-006 (doc-only, zero new tool code).

- **INV-METHOD-FLAG-SUMMARY-001. `MethodGroup`-kind static-table cells additively carry `MethodReturnFlagIds[]?` — the deduplicated, ordinal-sorted UNION of enum-flag member ids reachable in any `return` position of the delegate target's body, when the target carries a source declaration in the same compilation as the cell site.** Pre-fix a downstream rule-check joining a method-group cell against a flag-mask cell on the same row had no semantic surface to query "which mask values does this delegate produce?" — the caller had to walk syntax by hand or trust a name convention. The enrichment promotes that join from out-of-band heuristics to a Roslyn-grounded fact. The walker is pure semantic: it reads each `IReturnOperation.ReturnedValue` and reuses `TryCollectEnumFlagMembers` (the same classifier `EnumFlags`-kind cells already use) to accept a single enum-const `IFieldReferenceOperation` or an `|`-composed `IBinaryOperation` tree of enum-const leaves; every other return shape (method invocation, conditional expression, computed mask, literal zero) is silently skipped for that return path. Multi-path methods union the classified returns across all paths — the field surfaces the SET of flags reachable, never a guarantee that any specific flag is always returned (callers needing intersection / always-returned semantics build it later as a separate enrichment). Returns null in four cases, each load-bearing: target has no `DeclaringSyntaxReferences` (compiled-metadata BCL / dependency method), target's syntax tree is not in the site compilation's `SyntaxTrees` (cross-compilation enrichment deferred — surfaces `MethodGroupId` only), target's body produces no `IOperation` tree (e.g. partial / unimplemented), or every return-position classification falls back to Computed. Nested anonymous-function / local-function bodies are NOT walked — the `ReturnFlagCollector` overrides `VisitAnonymousFunction` + `VisitLocalFunction` to drop the recursion, because their `return` statements belong to the inner symbol, not the outer delegate target. The wire field is purely additive (existing callers reading only `MethodGroupId` keep working) and carries no Roslyn-version-sensitive shape — only the canonical ordinal-sorted ids the resolver already emits. Pinned by `StaticTableExtractorTests` (7 facts under the `INV-METHOD-FLAG-SUMMARY-001` region: single-flag return surfaces flag id, OR-composed return surfaces all leaves, multi-return-path union, computed return stays null, mixed classified+computed surfaces only the classified subset, nested-lambda return does not bleed through, duplicate flags across returns dedupe + ordinal sort, compiled-metadata target stays null) plus the augmented `GetStaticTables_MethodGroupCell_ClassifiedAsMethodGroup` pin that locks `MethodReturnFlagIds = null` for non-enum-returning targets. Closes LB-TRACK-20260514-005-v1.1.

## Diagnostic Envelope Preprocessor Scope (post-v0.7.3)

- **INV-DIAGNOSTIC-ENVELOPE-DEFINES-001. Every `lifeblood_diagnose` and `lifeblood_compile_check` response surfaces the preprocessor scope its diagnostics were bound under — `definesActive` (the active symbols, sorted ASCII-ordinal and deduplicated) plus `resolvedModule` (the compilation the scope resolved to, empty for project-wide).** Pre-fix a caller could not tell an Editor-only finding apart from a release-build risk without re-running the host under a different define set; `UNITY_EDITOR`-gated code surfaced the same wire shape as `UNITY_STANDALONE` code. The fix lifts the scope from "implicit in the request" to "explicit on every response" so a player-build readiness audit reads define context off one call instead of two. Scope rules mirror the legacy diagnostics rules: file-scope routes through `FindOwningCompilation` (same path-match contract as the rest of the adapter); module-scope uses the request's module name; project-wide returns the sorted-deduped union across every loaded compilation. Resolver duplication killed at the seam — `ResolveCompilation` was promoted to return `(string? Module, CSharpCompilation? Compilation)` so `CompileCheckSnippet` no longer carries an inline name-lookup, and `GetDiagnosticsReport` reuses `FindOwningCompilation` instead of an inline `goto resolved` walker. Domain: `DiagnosticsReport { Diagnostics, DefinesActive, ResolvedModule }` + `DefinesActive` on `CompileCheckResult`. Port: `ICompilationHost.GetDiagnosticsReport(DiagnosticsRequest)`. Adapter: private `CollectDefines(string? moduleName)` walks each compilation's first-tree `CSharpParseOptions.PreprocessorSymbolNames`. Handler: `WriteToolHandler.HandleDiagnose` + `HandleCompileCheck` route through the new report shape additively (no field removals from the legacy wire). Pinned by `DiagnosticEnvelopeDefinesTests` (7 facts: file-scope owning-module defines, module-scope defines, project-wide union, sort+dedup invariant, unknown-module empty-not-crash, compile-check file owning-module defines, compile-check snippet resolved-module defines). Closes LB-TRACK-20260514-002 / LB-INBOX-008.
